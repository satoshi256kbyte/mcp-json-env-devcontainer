{
  "name": "mcp-json-env-devcontainer",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "runArgs": [
    "--env-file",
    "${localWorkspaceFolder}/.env"
  ],
  "postCreateCommand": "bash -lc 'asdf install && uv venv --python \"$(asdf which python)\"'"
}
